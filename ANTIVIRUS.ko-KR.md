# 보조 프로그램 사용 목적과 백신 안내

[English](ANTIVIRUS.md) | 한국어

> [!WARNING]
> 현재 일부 백신 프로그램과 VirusTotal의 일부 검사 엔진에서 `BlockHudPowerShellHost.exe` 또는 관련 보조 스크립트를 바이러스, 트로이목마 등의 악성 파일로 탐지하는 사례가 확인되고 있습니다. 프로젝트는 이 탐지 현상을 인지하고 있으며, 원인이 될 수 있는 패키지 구성과 실행 경로를 계속 검토하고 있습니다.

DMeloper's Block HUD는 Rainmeter 스킨이지만, 모든 기능을 Rainmeter의 기본 기능과 Lua만으로 안전하고 일관되게 구현할 수는 없습니다.

따라서 일부 기능은 패키지에 포함된 `BlockHudPowerShellHost.exe`와 로컬 PowerShell 보조 스크립트를 사용합니다. 이 파일들은 외부에서 임의의 코드를 받아 실행하기 위한 프로그램이 아니라, Block HUD와 함께 배포된 기능을 실행하기 위한 런타임 구성 요소입니다.

## 왜 보조 프로그램이 필요한가요?

Rainmeter는 화면 표시, 미터 갱신, 클릭 동작과 스킨 상태 관리에는 적합하지만 다음과 같은 Windows 작업에는 한계가 있습니다.

- Windows 파일 및 폴더 선택 창 표시
- 이미지 파일 선택과 변환
- 여러 파일을 안전하게 복사하거나 교체하는 작업
- ZIP 패키지 다운로드, 검사 및 압축 해제
- 기존 데이터의 호환성 확인과 선택적 병합
- 실패 시 백업 파일로 복구하는 작업
- 별도 창을 사용하는 설정 및 관리 기능
- 실행 결과와 오류 정보를 구조화하여 Rainmeter로 반환하는 작업

`BlockHudPowerShellHost.exe`는 Rainmeter와 이러한 Windows 보조 작업 사이를 연결합니다. 직접 `powershell.exe` 창을 반복해서 생성하는 대신 Windows PowerShell을 호스트 내부에서 실행하고, 작업 결과를 다시 Block HUD에 전달합니다.

## 실제 사용 사례

### Settings와 Editor

Settings와 Editor에서는 다음 작업에 보조 프로그램이 사용될 수 있습니다.

- 프로그램, 파일 또는 폴더 경로 선택
- 아이템에 사용할 이미지 선택
- 선택한 아이콘이나 이미지의 처리 및 픽셀화
- Windows 파일 선택 창이 필요한 편집 기능
- 설정값을 관련 스킨과 데이터 파일에 즉시 적용
- 여러 설정 파일을 일관된 상태로 갱신
- 작업 실패 시 오류 결과를 Settings 또는 Editor에 전달

파일 선택 창은 Windows의 STA 환경을 요구할 수 있으므로 단순한 Rainmeter 명령만으로 안정적으로 대체하기 어렵습니다.

### Skin manager와 업데이트

Skin manager와 updater에서는 다음 작업에 사용됩니다.

- 공식 GitHub Release 정보 확인
- 설치된 Korea 또는 Global 패키지 식별
- 해당 variant와 일치하는 업데이트 파일 선택
- 업데이트 패키지 다운로드
- SHA256과 패키지 구조 검사
- 임시 폴더에서 안전하게 압축 해제
- 기존 설치 상태 백업
- 검증된 파일로 교체
- 실패 시 기존 상태로 rollback
- 업데이트 완료 후 필요한 Rainmeter 스킨 다시 불러오기

업데이트 도중에는 다운로드, 압축 해제, 검증, 백업과 복구가 하나의 작업 단위로 처리되어야 합니다. Rainmeter의 일반적인 클릭 명령만으로는 이 과정을 안전하게 보장하기 어렵습니다.

### 버전 설치 및 전환

여러 버전을 관리하는 기능에서는 다음 작업에 사용됩니다.

- 선택한 버전의 release 조회
- 비활성 설치 위치에 패키지 준비
- 기존 버전과 데이터 호환성 확인
- 전환 가능한 설치 상태인지 사전 검사
- 활성 버전 전환
- 전환 실패 시 기존 활성 버전 유지
- 완료 후 필요한 Rainmeter 구성 다시 로드

### 기존 데이터 가져오기

이전 버전의 데이터를 가져올 때는 다음 작업이 필요합니다.

- 이전 설치 위치 확인
- 가져올 수 있는 데이터인지 호환성 검사
- 사용자 이미지와 설정 데이터 분류
- 현재 버전의 기본 데이터와 사용자 데이터를 구분
- 가져오기 전 백업 생성
- 허용된 데이터만 선택적으로 병합
- 실패 시 원래 상태 복구
- 결과와 로그 위치를 Block HUD에 전달

단순 파일 덮어쓰기는 현재 설정이나 사용자 데이터를 손상시킬 수 있으므로 별도의 검증 및 rollback 과정이 필요합니다.

### 설치 후 초기화

설치, 업데이트 또는 버전 전환 직후에는 다음 준비 작업이 실행될 수 있습니다.

- 패키지에 필요한 보조 파일이 존재하는지 확인
- 실행 파일과 스크립트의 일치 여부 확인
- 사용자별 런타임 호스트 준비
- 백신 검사가 끝날 때까지 초기화 진행 상태 표시
- Settings, Skin manager와 기타 보조 기능이 실행 가능한지 확인

이 과정에서 Windows 보안 또는 백신이 새 실행 파일과 PowerShell 관련 파일을 처음 검사하면 초기화가 일시적으로 느려질 수 있습니다.

### Jukebox

Jukebox에서는 다음과 같은 Windows 작업에 보조 프로그램이 사용될 수 있습니다.

- 로컬 오디오 파일과 폴더 선택
- 재생에 사용할 파일 경로 처리
- 플레이어 관련 상태와 작업 결과 전달
- 외부 음악 앱 연동에 필요한 보조 작업
- 재생 오류와 보조 프로그램 오류 기록

### 진단과 로그

문제가 발생했을 때는 다음 작업에도 사용됩니다.

- 관련 로그 파일 생성
- 여러 보조 작업의 결과 수집
- 오류 상태와 로그 위치를 Rainmeter에 반환
- Settings 또는 Skin manager에서 로그 폴더 열기
- 설치 및 업데이트 상태 진단

## 백신 경고가 발생하는 이유

현재 확인된 문제는 다음과 같은 특성이 백신의 행위 또는 평판 기반 검사 대상과 겹칠 수 있기 때문입니다.

- Rainmeter에서 PowerShell 자동화 기능 실행
- 새로 빌드되어 파일 평판이 충분하지 않은 실행 파일
- 여러 로컬 스크립트를 실행하는 전용 호스트
- 파일 복사, 압축 해제, 백업과 교체 작업
- 업데이트를 위한 네트워크 및 파일 작업
- 콘솔 창 없이 실행되는 보조 프로그램
- 설치 직후 처음 확인되는 실행 파일과 스크립트 조합

이로 인해 설치 또는 업데이트 직후 초기화가 지연되거나, 일부 백신 및 VirusTotal 검사 엔진에서 정상 구성 요소가 바이러스, 트로이목마 등의 악성 파일로 표시될 수 있습니다. 탐지 이름이나 탐지 엔진의 수만으로 파일이 안전하거나 악성이라고 단정할 수는 없으므로, 반드시 배포 출처와 체크섬을 함께 확인해야 합니다.

## 코드 서명 관련 한계

코드 서명은 실행 파일의 게시자와 서명 이후 변경 여부를 확인하는 데 도움이 되고 파일 평판 형성에도 유리할 수 있지만, 모든 백신 경고가 사라지는 것을 보장하지는 않습니다.

DMeloper's Block HUD는 사용자에게 비용을 받지 않고 개발·배포·운영되는 완전 무료 프로젝트입니다. 개발자가 연간 약 20만 원에 달하는 코드 서명 인증서와 서명 유지 비용을 지속적으로 부담하기 어려워 현재 릴리스의 `BlockHudPowerShellHost.exe`에는 디지털 서명을 적용하지 못하고 있습니다.

프로젝트는 서명되지 않은 파일이라는 사실을 숨기지 않으며, 공식 배포 경로, release notes의 SHA256과 배포 검증 절차를 통해 사용자가 파일의 출처와 무결성을 확인할 수 있도록 유지합니다. 비용과 운영 여건이 달라지면 실행 파일 서명을 다시 검토할 예정입니다.

## 안전하게 확인하기

Block HUD는 [공식 GitHub Releases 페이지](https://github.com/d-meloper/dmelopers-block-hud/releases/latest)에서만 다운로드하세요.

다운로드한 `.rmskin` 파일의 SHA256은 다음과 같이 확인할 수 있습니다.

```powershell
Get-FileHash ".\DMelopers-Block-HUD_Korea.rmskin" -Algorithm SHA256
Get-FileHash ".\DMelopers-Block-HUD_Global.rmskin" -Algorithm SHA256
```

계산된 체크섬이 해당 release notes에 게시된 값과 정확히 일치해야 합니다. 체크섬이 다르거나 배포 출처를 확인할 수 없다면 해당 파일을 실행하지 말고 공식 Releases 페이지에서 다시 다운로드하세요. 이 안내는 Windows 보안 또는 백신 기능을 끄거나 Block HUD 폴더 전체를 예외로 등록하라는 의미가 아닙니다.

## 향후 개선 계획

프로젝트는 향후 업데이트를 통해 다음 개선을 계속 적용할 예정입니다.

- PowerShell 보조 작업의 실행 경로 단순화
- 직접적인 `powershell.exe` fallback을 사용하지 않는 구조 유지
- 중첩된 encoded-command 실행을 사용하지 않는 구조 유지
- WMI/CIM 기반 프로세스 검색을 사용하지 않는 구조 유지
- 패키지에 포함된 로컬 스크립트만 명시적으로 실행하도록 실행 계약 강화
- 실행 파일의 제품명, 제작자, 버전과 원본 파일명 등 식별 정보 관리
- 빌드된 실행 파일과 소스, 버전 및 SHA256의 일치 여부 검증
- 공식 release notes를 통한 배포 패키지별 SHA256 공개 유지
- 설치와 업데이트 과정의 불필요한 반복 실행 축소
- Windows 보안 또는 백신 검사 중 진행 상태를 명확하게 표시
- 백신의 행위 기반 탐지를 유발할 수 있는 실행 방식의 지속적인 축소

이 문제는 사용자에게 보안 기능 해제나 광범위한 예외 등록을 요구하는 방식이 아니라, 보조 프로그램의 실행 구조를 지속적으로 개선하고 검증 가능한 공식 배포 파일을 제공하는 방식으로 완화할 예정입니다.
